import { SimpleTabScreen } from '@/features/tabs/screens/simple-tab-screen';

export default function FavouriteScreen() {
  return (
    <SimpleTabScreen
      title="Favourite"
      description="Keep saved products ready for later."
    />
  );
}
