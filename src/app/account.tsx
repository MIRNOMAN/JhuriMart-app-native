import { SimpleTabScreen } from '@/features/tabs/screens/simple-tab-screen';

export default function AccountScreen() {
  return (
    <SimpleTabScreen
      title="Account"
      description="Manage profile details and preferences."
    />
  );
}
